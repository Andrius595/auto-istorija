import {Roles} from "~/enums/roles";

export const useRoles = () => {
    // const { data } = useAuth()

    // const isClient = (): boolean => {
    //     return data.value?.user.roles.includes(Roles.CLIENT) ?? false
    // }
    //
    // const isServiceEmployee = (): boolean => {
    //     return data.value?.user.roles.includes(Roles.SERVICE_EMPLOYEE) ?? false
    // }
    //
    // const isSystemAdmin = (): boolean => {
    //     return data.value?.user.roles.includes(Roles.SYSTEM_ADMIN) ?? false
    // }
    //
    //
    // return { isClient, isServiceEmployee, isSystemAdmin }
}